import{useState,type FormEvent}from'react';import{useQuery}from'@tanstack/react-query';import{Navigate,Route,Routes,useNavigate}from'react-router-dom';import{Activity,ArrowRight,LockKeyhole,ShieldCheck}from'lucide-react';import{api}from'./api';import{Brand,ErrorState,Loading,Shell}from'./components';import{DoctorDashboard,PatientDashboard}from'./dashboards';import type{Role}from'./types';
function Auth({register=false}:{register?:boolean}){const navigate=useNavigate();const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const data=new FormData(e.currentTarget);try{if(register)await api.register({full_name:String(data.get('name')),email:String(data.get('email')),password:String(data.get('password')),role:String(data.get('role'))as Role});await api.login(String(data.get('email')),String(data.get('password')));const me=await api.me();navigate(me.role==='PATIENT'?'/patient/dashboard':'/doctor/dashboard')}catch(err){setError(err instanceof Error?err.message:'Unable to continue')}finally{setBusy(false)}}return <div className="auth"><section className="auth-story"><Brand/><div><span className="eyebrow">CONTINUOUS CARE, CLEARLY PRESENTED</span><h1>Vital context.<br/>Thoughtful decisions.</h1><p>A focused remote-monitoring workspace that turns incoming physiological readings into a clear, accountable care view.</p><div className="trust"><ShieldCheck/><span><strong>Privacy-minded by design</strong><small>Role-scoped access and auditable actions</small></span></div></div><small>Academic software prototype · Not a medical device</small></section><section className="auth-form"><form onSubmit={submit}><div className="form-icon"><Activity/></div><span className="eyebrow">PULSEBRIDGE ACCESS</span><h2>{register?'Create your account':'Welcome back'}</h2><p>{register?'Set up secure access to the prototype workspace.':'Sign in to continue to your monitoring workspace.'}</p>{register&&<><label>Full name<input name="name" required minLength={2} autoComplete="name"/></label><label>Account type<select name="role"><option value="PATIENT">Patient</option><option value="HEALTHCARE_PROFESSIONAL">Healthcare professional</option></select></label></>}<label>Email address<input name="email" type="email" required autoComplete="email"/></label><label>Password<input name="password" type="password" required minLength={10} autoComplete={register?'new-password':'current-password'}/></label>{error&&<div className="form-error" role="alert">{error}</div>}<button className="primary" disabled={busy}>{busy?'Please wait…':register?'Create account':'Sign in'}<ArrowRight/></button><div className="switch">{register?'Already registered?':'New to PulseBridge?'} <button type="button" onClick={()=>navigate(register?'/login':'/register')}>{register?'Sign in':'Create account'}</button></div><div className="secure-note"><LockKeyhole/> Credentials are protected with Argon2id hashing.</div></form></section></div>}
function Protected(){const user=useQuery({queryKey:['me'],queryFn:api.me,retry:false});if(user.isLoading)return <Loading/>;if(user.isError||!user.data)return <Navigate to="/login"/>;return <Shell user={user.data}><Routes><Route path="/patient/dashboard" element={user.data.role==='PATIENT'?<PatientDashboard user={user.data}/>:<Navigate to="/doctor/dashboard"/>}/><Route path="/doctor/dashboard" element={user.data.role==='HEALTHCARE_PROFESSIONAL'?<DoctorDashboard/>:<Navigate to="/patient/dashboard"/>}/><Route path="*" element={<Navigate to={user.data.role==='PATIENT'?'/patient/dashboard':'/doctor/dashboard'}/>} /></Routes></Shell>}
export default function App(){return <Routes><Route path="/login" element={<Auth/>}/><Route path="/register" element={<Auth register/>}/><Route path="/*" element={api.hasToken()?<Protected/>:<Navigate to="/login"/>}/></Routes>}
